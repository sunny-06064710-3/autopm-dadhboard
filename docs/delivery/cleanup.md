# GitHub 整理记录：2026-09-23

目标：从[根首页](../../README.md)找到行动、问题、证据及原任务；不再维护互相冲突的状态副本。本记录只说明仓库整理，未修改Airtable。

## 已整理的内容

- 首页改为业务导航；增加行动页、审计发现与受限证据索引、架构说明及证据边界，统一对应原Issue。
- 协作页收敛为当前约定与未合并PR导航，旧分支和成果不冒充正式交付。
- 从当前文件树移除47个文件：31个混合个人历史文件、6个旧调试摘录、9个前端临时补丁/备份、1个过时GitHub导航JSON，合计3,158,768字节。
- 每个移除文件均先与基准提交核对、保存本地副本并校验SHA-256；[逐文件清单](cleanup-manifest.json)保留原路径、字节、哈希、Git blob及恢复提交。Windows换行转换可能令工作区SHA-256与Git blob原字节不同，二者用途分开。
- [证据清单](evidence-manifest.json)保存审计包总量、完整私有清单哈希及23份报告/检查源码哈希；不是把原始业务数据上传公开仓库。

## 为什么这些能清，哪些保留

前端临时文件仅用于一次性文本替换或备份，现行main.py挂载frontend目录，首页没有引用这些文件；仓库中发现的临时文件间引用随同一组移除。这是仓库消费者检查，不证明外部用户从未保存过这些临时文件的URL。

当前服务源码、首页、JS、workflow、历史版本页面、seed脚本均保留。其他前端目录用途未查明，不仅按旧名字删除。移除的备份不代表服务功能更改，未执行部署。

保留planning、历史Issue草稿和来源快照：它们支撑任务及验收溯源，已有“历史”边界；未合并Qoder/演示/导入PR仍引用既有输入，不作破坏性搬移。保留原Issue、评论、PR与Git历史；没有批量关任务、删除远端分支或接受待审结果。

原混合个人上下文从当前树移除，项目约定由根AGENTS和当前协作页承接；若旧任务需要历史细节，按清单恢复指定文件，不默认把整套私人记忆发给执行AI。

## 恢复方式

基准提交：`29eead8b3fc1063fafd01ac3739ce5a76e2d142b`。在独立干净分支，按清单逐个选择文件，可使用：

```text
git restore --source=29eead8b3fc1063fafd01ac3739ce5a76e2d142b -- <清单中的具体路径>
```

恢复前确认目标路径无未提交工作；不要批量覆盖当前文件。完整原字节副本由本次维护者在仓库外保留。Git历史未重写；仓库当前公开，移除HEAD中的私人材料不等于从历史访问中删除。

## 验证和发布边界

运行 `python docs/delivery/check_hub.py` 检查现行导航、现行导航及47个移除对象；再运行 `git diff --check`。它是文档整理校验，绝非Airtable健康测试。

本次沿用PR #44，不新建第二个管理PR。合并前main仍是旧入口；合并后默认首页才是正式唯一入口。其他PR保留各自审批和验收边界，不能为清理仓库强行合并。
