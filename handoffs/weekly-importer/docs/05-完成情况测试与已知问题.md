# 完成情况、测试与已知问题

## 1. 已完成能力

| 能力 | 状态 | 说明 |
|---|---|---|
| 周报本地解析 | 已实现 | 读取项目块、字段、时间轴、颜色、Issue 和来源单元格 |
| 可选 DeepSeek 解析 | 已实现 | 模型只提供候选内容，不拥有目标选择权 |
| Airtable schema 动态核对 | 已实现 | 稳定 ID、类型、Link 目标、选项和 schema hash |
| 周报写入预览 | 已实现 | 逐记录/逐字段变化、提示、阻断和身份守卫 |
| Projects/Tasks/Issues 写入 | 已实现 | 非关键字段可局部跳过，关键身份/版本继续阻断 |
| Engineering Remark 增量历史 | 已实现 | 保留人工备注、旧格式兼容、同内容幂等 |
| 写前重读和写后回读 | 已实现 | 防并发覆盖和不确定重试 |
| Airtable -> All Tracker | 已实现 | 单元格级预览、另存副本、绿色标记、回写基线 |
| All Tracker -> Airtable | 已实现 | 仅绑定字段、非空变更、三方冲突保护 |
| 企业代理 TLS | 已修复 | 使用 Windows 系统信任库，不关闭验证 |
| 新电脑主库默认映射 | 已实现 | 只对已确认 Base 使用，运行时仍核对 schema |
| 单文件 EXE | 有历史测试版 | 本交接未附 EXE，源码优先 |

## 2. 已完成的重要修复

### TLS 证书不受信任

公司浏览器能访问 Airtable，但早期程序使用的证书来源不兼容企业 HTTPS 代理。修复后使用系统 ROOT/CA，并保留完整 TLS 验证。

### 新电脑映射缺失

只为确认的主 Base 提供非敏感默认映射，例如 Engineering Remark 版本存储、Task/Issue 项目 Link 和 Factory code。其他 Base 不套用固定 ID。

### 富文本写入成功却误报失败

Airtable 会重排 Markdown 有序列表编号并增加尾部换行。旧程序逐字比较后报告失败，用户重试又可能重复追加。rc4 在共享比较和合并路径中只规范列表标记格式，正文变化仍会被发现。

## 3. 历史验证证据

- 2026-09-19：全量自动测试 428 项通过。
- Shark 20260918：185 个项目块；旧预览 83 个操作本地回放完成；重复执行新增写入 0。
- Ninja 20260918：176 个项目块；旧预览 168 个操作本地回放完成；重复执行新增写入 0。
- 两份数据都验证了 Airtable -> All Tracker、修改两个绑定字段后回写、再次回写 0 变化，以及云端和本地同时修改时阻断。
- 回放基于用户实际日志和云端快照的本地副本，没有新增正式 Airtable 测试写入。

详细历史见 `docs/reference/本地修复验证_20260919.md` 和相关报告。

## 4. P0：业务模型尚未闭环

业务要求周报内容按 Project 管理，SKU 维度从 Airtable 开始。当前代码却先用编号 + SKU + 工厂把来源项目展开到管理单元，再写 Project/Task/Issue。

风险：同一 Project 有多个 SKU/工厂时，项目级内容可能重复或挂到错误层级。

必须完成：

1. 确认 Project 父对象或 Project Group 的唯一身份。
2. 明确哪些字段属于 Project、哪些属于 SKU/工厂单元。
3. Task/Issue 只关联 Project 层，不因 SKU 数量复制。
4. All Tracker 仍按 Project Dashboard 展示，SKU 行只作为展示或单位绑定。
5. 增加同 Project 多 SKU、多工厂的验收用例。

## 5. 已修复：All Tracker 纯时间值导致预览失败（rc5）

2026-09-21 诊断结论：

- Airtable 网络请求成功，故障发生在生成本地总表预览时。
- 文件：`All Projects Tracker V2 (1).xlsx`。
- 单元格：`ALL PROJECTS!AC695`，列标题 `FOT`。
- openpyxl 读取类型：`datetime.time`，值为 `00:00:15.231000`，单元格显示格式为日期 `dd-mmm-yy`。
- 该行 `Project Number` 为空，是一个落在有效读取范围内的异常/游离值。
- `source/autopm/tracker.py::scalar()` 只处理 `datetime` 和 `date`，随后 `read_tracker()` 对所有非公式值执行 `json.dumps` 计算内容哈希，触发 `Object of type time is not JSON serializable`。
- 同目录其他四个主要 All Projects Tracker 文件没有 `time` 值，`read_tracker()` 可正常完成。

2026-09-21 修复内容：

1. `scalar()` 将 `datetime.time` 转为 ISO 文本、`timedelta` 转为保留跨天和微秒的稳定文本；沿用已有完整日期处理及指纹结构。
2. 日期语义列中的时间／时长给出工作表、坐标和表头警告，导出预览与回写预览均保留该诊断，不推测日期。
3. 未修改格继续复制原始 XML；若云端有效完整日期更新一个纯时间格式格，仅将该目标格改为完整日期格式。
4. 新增 7 项合成回归，覆盖孤立行、有效项目行、精度、指纹、原生 XML、旁路审计、回写基线和日期显示格式。

本轮在隔离的原始提交上复现序列化错误，再在修复版通过全部用例。用户本轮指定的总表可正常读取，使用本轮只读取得的 Airtable 快照生成副本并更新 406 个单元格；原表保持不变，19,505 个公式保留，再次导出和未编辑回写预览均为 0 变更。上述历史 V2 原文件本轮未提供，未声称对它重新实测。详见 [更新日志](../CHANGELOG.md)。

## 6. 其他已知差距

- Project 表和 Task 表可能同时保存部分里程碑日期，双处日期的最终权威和同步策略仍需确认。
- All Tracker 只覆盖部分项目字段和大里程碑，不是 Airtable 全量镜像。
- 隐藏在 Excel 超链接目标中的 URL 仍可能无法被本地解析器读取。
- 未明确的人员、工厂、日期、Select 选项和重复任务继续以提示或跳过处理。
- DeepSeek 解析质量不能代替身份、字段、日期和目标记录验证。
- 历史 `legacy-reference/` 使用项目名称匹配，与当前严格规则不同，不应恢复为生产路径。

## 7. 已修复：P/Q/R 保护和备注空代码围栏（rc6）

- P/Q/R 按模板位置保护，不依赖表头文字；Airtable 导出和 All Tracker 回写均不会生成这三列的变更。
- Engineering Remark 末尾至少两个仅含反引号的空围栏会被清理；含正文的真实代码块和单个反引号围栏保持原样。
- 真实文件重放输出 371 个允许更新，移除旧计划中的 55 个 P 列更新；P/Q/R 单元格 XML 与原表完全一致，40 条备注清除空围栏。
- 历史 Airtable 记录未做无审核的批量改写；这些记录在读取到 All Tracker 时会被清理，之后被周报正常更新时也会清理。

## 8. 当前交接分支验证

rc6 修复后：438 项 Python 测试通过（59.143 秒）。凭据及真实数据仅保留在本地，真实 Airtable 写入为 0。下方为 rc4 交接时的历史验证。

2026-09-21 对本次复制的源代码重新运行：

```text
Ran 428 tests in 53.149s
OK

Browser extension: 27 tests passed, 0 failed
Runtime self-check: all checks passed
```

Git 交接目录共 152 个文件，约 18.31 MB，其中约 16.95 MB 为随程序使用并附许可证的 Noto Sans SC 字体。敏感信息扫描只命中示例占位符和测试中的明确假 Token 文本，没有发现真实 PAT、API Key 或 Bearer Token。

交接包的 `_verify_runtime.py` 已改为调用生产 `_extract_narrative_date`，修正原脚本因复制了一份过期简化逻辑而对 `Dec 1st` 误报失败的问题；该调整不改变业务代码。

新增入口和 8 份交接文档已通过 `git diff --cached --check`；复制保留的历史报告和源代码中仍有原有行尾空格，不为清理格式而改变交付源码。29 个 Markdown 文件的相对链接检查无缺失。推送后还需用远端 ref 核对提交 SHA。
