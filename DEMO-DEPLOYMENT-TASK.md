# DEMO-DEPLOY-001：部署 AutoPM 本地体验网站

## 目标

将 `demos/autopm-local-workspace` 部署为供获准评审者访问的演示网址。它是业务交互原型，不连接 Airtable、Power Apps、ECN 或真实通知。

## 已提供的源码与已知事实

- 启动：`npm start`；验证：`npm test`。
- 服务是 Node.js 标准库 HTTP 服务，入口为 `server.mjs`。
- 页面调用 `GET /api/state`、`POST /api/operation` 和 `POST /api/export`，因此 GitHub Pages 无法完整承载。
- 当前本地版本仅监听 `127.0.0.1`，并把用户操作写入 `data/state.json`；该文件、备份和导出文件均已忽略，不能提交。
- 种子数据在 `data/seed.json`，只含演示场景；不得接入真实项目数据、账号凭据或生产接口。

## 开始前必须在 Issue 中确认

1. 选定可运行 Node 服务的托管环境，并说明费用、访问范围、停用方式和负责人。
2. 选定数据策略：
   - **推荐：只读演示。** 每次启动从种子数据开始，禁止或明确提示会改写数据的操作。
   - 如需保存操作记录或多人使用，先取得数据保存期限、访问控制和清理方式的明确批准。
3. 不得因部署自行扩大为 Airtable/Dataverse/Power Apps 集成或生产切换。

## 交付要求

1. 在独立分支实现，提交 PR，不直接合并。
2. 让服务按托管环境需要监听地址与端口；保留本地 `npm start` 的可用性。
3. 在 PR 中提供：演示 URL、部署配置、数据策略、回滚/停用步骤，以及首页和 `/api/state` 的实际访问证据。
4. 在本 Issue 记录已完成项、提交/PR、实际测试、未验证项、阻塞和下一步；不自行关闭 Issue 或合并 PR。

## 验收标准

- 获准访问者能打开首页；六个中心的示例内容可读取。
- 运行 `npm test` 通过；线上首页和 `/api/state` 返回成功。
- 仓库和部署环境不含 `data/state.json`、导出文件、账号凭据或真实业务数据。
- 数据保存、访问和停用方式与开始前确认的策略一致。
